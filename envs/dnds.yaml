# Conda environment file for dn_ds analysis

name: dnds
channels: [bioconda, conda-forge]
dependencies:
  - python=3.10
  - requests
  - mafft            # alinhamento de proteínas
  - pal2nal          # retro‑tradução p/ códons
  - biopython        # utilidades Python